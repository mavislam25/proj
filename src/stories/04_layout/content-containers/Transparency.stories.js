import { Dialog } from "@material-ui/core";
import React from "react";

export default {
  title: "Layout/ContentContainers/Transparency",
  component: Dialog,
};

export const ModalScreen = () => <Dialog open />;
