import SvgIcon from "@material-ui/core/SvgIcon";
import withStyles from "@material-ui/core/styles/withStyles";

const MerchTeamIcon = withStyles(() => ({
  root: { color: "#2BBFBE" },
}))(SvgIcon);

export default MerchTeamIcon;
